# Secure-Access-Control-System-with-On-Board-Cryptographic-Hashing-
The Secure Access Control System with On-Board Cryptographic Hashing is an FPGA-based authentication system designed in Verilog. It captures a user password through a USB keyboard, computes its SHA-1 or SHA-256 hash in hardware, and compares it with a pre-stored master hash for verification.
